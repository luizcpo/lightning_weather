# Copy this file to `.env` and fill in your real key.
# The key needs the following Google Cloud APIs enabled:
#   - Maps JavaScript API   (used by the front-end autocomplete)
#   - Places API            (used by the front-end autocomplete)
#   - Geocoding API         (used by GeocodingService on the server)
GOOGLE_MAPS_API_KEY=
